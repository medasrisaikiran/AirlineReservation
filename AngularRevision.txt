angular cli
	//commands
	npm install
	 |
	 |
	node project manager (manages all the files during creation)

//node.js command prompt ->ng new App->visualstudiocode->new terminal-->npm start-->ng serve


//new component
ng g c "name"

//new service
ng g service "service"

//new pojo					
ng g class "name"



//styles of forms in angular
1 template driven
2 form control


//component.ts			//component.html
  Flightid				[(ngModel)]="FlightId"	//two way binding
 FlightId				[FlightId]="FlightId"		//one way binding
 showFlight(){}			(blur)="showFlight()"		//event binding




//directives
1 *ngIf
2 *ngFor