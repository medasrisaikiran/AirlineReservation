import { FlightBySrcAndDestDto } from './flight-by-src-and-dest-dto';

describe('FlightBySrcAndDestDto', () => {
  it('should create an instance', () => {
    expect(new FlightBySrcAndDestDto()).toBeTruthy();
  });
});
