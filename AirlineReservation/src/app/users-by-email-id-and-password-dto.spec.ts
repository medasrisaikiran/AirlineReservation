import { UsersByEmailIdAndPasswordDto } from './users-by-email-id-and-password-dto';

describe('UsersByEmailIdAndPasswordDto', () => {
  it('should create an instance', () => {
    expect(new UsersByEmailIdAndPasswordDto()).toBeTruthy();
  });
});
